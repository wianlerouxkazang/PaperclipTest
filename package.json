{
  "name": "paperclip-test-todo-app",
  "version": "1.0.0",
  "private": true,
  "description": "MVP to-do app with automated browser QA via Playwright",
  "scripts": {
    "qa:browser": "playwright test",
    "qa:browser:headed": "playwright test --headed",
    "qa:browser:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "http-server": "^14.1.1"
  }
}
